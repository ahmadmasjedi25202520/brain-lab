name: Run Python Hello World

on:
  workflow_dispatch:

jobs:
  run-python-script:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line Python script
        run: python -c "print('Hello, World from inline script!')"
